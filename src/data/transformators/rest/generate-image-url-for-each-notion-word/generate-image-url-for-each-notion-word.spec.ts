import { functionImportTest } from '@infrastructure/utils';

import { generateImageUrlForEachNotionWord } from './generate-image-url-for-each-notion-word.function';

describe('generateImageUrlForEachNotionWord function', () => {
  functionImportTest(generateImageUrlForEachNotionWord);

  it('', () => {});
});
