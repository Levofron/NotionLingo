import { functionImportTest } from '@infrastructure/utils';

import { isClient } from './is-client.function';

describe('isClient function', () => {
  functionImportTest(isClient);

  it('', () => {});
});
